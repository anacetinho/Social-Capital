# API URL for the frontend
# For Docker deployment, use relative path (default)
REACT_APP_API_URL=/api/v1

# For local development without Docker, use:
# REACT_APP_API_URL=http://localhost:5000/api/v1
